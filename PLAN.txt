# PLAN: Fix API Connection for Dashboard Recommendations & Tomorrow's Energy Forecast

## THE PROBLEM
API cannot be reached at /optimize or /predict. Need to discover correct endpoints and fix inputs.

## STRICT RULES - READ FIRST

✓ ONLY modify API calls and data fetching logic
✓ DO NOT change any UI components, layouts, or styling
✓ DO NOT change how data is DISPLAYED - only how data is FETCHED
✓ DO NOT modify Dashboard page structure - only the API call inside it
✓ DO NOT modify Energy Forecast page UI - only the data source
✓ DO NOT touch Maintenance page
✓ DO NOT touch AI Assistant page
✓ DO NOT change routing or navigation
✓ If existing code works, LEAVE IT ALONE
✓ Only add error handling, don't remove existing error handling
✓ Test that existing features still work after each change

## STEP 1: DISCOVER CORRECT API URL

Test these URLs WITHOUT changing any code first. Use browser console only.

URL OPTION A:
https://DevNumb-MLYorkchillerOptimzer.hf.space

URL OPTION B:
https://huggingface.co/spaces/DevNumb/MLYorkchillerOptimzer

For each URL, test:
- GET /health
- GET /
- POST /predict

## STEP 2: DISCOVER CORRECT INPUT FORMAT

The model expects 12 features. Find the exact format.

Try format A (array):
{
  "features": [1200, 18.6, 6.5, 90, 10, 5, 0, 1, 1, 0, 0, 2]
}

Try format B (named):
{
  "cooling_load": 1200,
  "wet_bulb_temp": 18.6,
  "chw_setpoint": 6.5,
  "current_limit": 90,
  "hour": 10,
  "month": 5,
  "weekend": 0,
  "chiller1": 1,
  "chiller2": 1,
  "chiller3": 0,
  "chiller4": 0,
  "total_chillers": 2
}

Try format C (DataFrame):
{
  "data": [[1200, 18.6, 6.5, 90, 10, 5, 0, 1, 1, 0, 0, 2]]
}

## STEP 3: FIX API SERVICE FILE

In src/services/api.ts (or create if doesn't exist):

ONLY change:
- Base URL to use working endpoint
- Request body format to match working format
- Response parsing to match actual response

DO NOT change:
- Any other functions
- How existing components use the API
- Any UI or display logic

## STEP 4: FIX DASHBOARD RECOMMENDATION API CALL

In src/pages/Dashboard.tsx:

Find where it calls the optimizer API. ONLY change:
- The URL being called
- The request body structure
- How it extracts kW/TR from response

DO NOT change:
- How recommendations are displayed
- Any styling or layout
- Any other calculations
- Any other part of Dashboard

## STEP 5: IMPLEMENT TOMORROW'S FORECAST

In src/pages/EnergyForecast.tsx:

ONLY add new functions for fetching tomorrow's data. DO NOT remove existing code.

Add:
- Function to get tomorrow's hourly predictions using same API
- Keep existing display code intact
- If adding new display elements, add them BELOW existing content
- DO NOT remove or replace existing charts or data

## STEP 6: ADD FALLBACK FOR API FAILURE

Add error handling that:
- Shows error message but keeps page working
- Uses last successful data if available
- Does NOT crash or blank out the page

## STEP 7: UPDATE ENVIRONMENT VARIABLE

In .env file:
VITE_OPTIMIZER_URL=https://DevNumb-MLYorkchillerOptimzer.hf.space

DO NOT change any other environment variables.

## VERIFICATION CHECKLIST - ALL MUST WORK AFTER CHANGES

Dashboard Page:
☐ Still loads without errors
☐ All existing charts display
☐ All existing data shows
☐ Recommendation card still visible
☐ ONLY the API data should be different (savings now correct)

Energy Forecast Page:
☐ Still loads without errors
☐ Existing forecast displays
☐ New tomorrow forecast added (not replacing existing)
☐ Page layout unchanged

Other Pages:
☐ Maintenance page works (no changes made)
☐ AI Assistant page works (no changes made)
☐ Navigation between pages works

## ROLLBACK PLAN

If anything breaks:
1. Revert only the API URL change
2. Keep using original hardcoded URL
3. Report what broke

## WHAT TO REPORT

After fixing, tell me:
1. Which URL pattern worked
2. Which input format worked  
3. Dashboard still displays all existing data
4. No other pages were affected
5. What exactly was changed (list files and lines)

## START

Open browser console on Hugging Face Space. Run fetch tests WITHOUT changing code. When you find working URL/format, ONLY update the API service file. Test Dashboard still works. Then add tomorrow forecast.